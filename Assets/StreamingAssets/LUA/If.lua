-- base if func, provided by Delivr

function func(A, B)
    if A >= B then
        return true
    elseif op == "-" then
        r = a - b
    elseif op == "*" then
        r = a * b
    elseif op == "/" then
        r = a / b
    else
        error("invalid operation")
    end
end

return fact(5)